# Plan 01 placeholder only.
# Plan 03 keeps Alembic configured, but no business migrations are defined here.
# Do not run migrations automatically; use only after explicit user action.

[alembic]
script_location = migrations
prepend_sys_path = .
sqlalchemy.url = mysql+pymysql://placeholder:placeholder@127.0.0.1:3306/hermes_btc_agent
